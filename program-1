#include<stdio.h>
#include<math.h>
int main()
{
	int num1,num2;
	printf("enter numbers:" );
	scanf("%d%d",&num1,&num2);
	if(num1>num2)
	printf("u got it");
	else
	{
		printf("try again");
		
	}
	return 0;
}
