#include<stdio.h>
int main()
{
	int i,n,a[10];
	printf("enter n value:");
	scanf("%d",&n);
	printf("enter %d elements\n",n);
	for(i=0;i<n;i++)
	{
	  scanf("%d",&a[i]);
	  printf("the reverse of an array is:\n");
     }
	  for(i=n-1;i>=0;i--)
	  printf("%d\t",a[i]);
	  return 0;	
	
}
